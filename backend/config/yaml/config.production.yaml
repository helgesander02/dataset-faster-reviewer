server:
  port: "8080"
  host: "localhost"

static:
  root_folder: "/app/example_root/image_root/unverify"
  backup_folder: "/app/backups"

database:
  host: ""
  port: ""
  username: ""
  password: ""
  database: ""

logging:
  level: "debug"
  format: "console"

cors:
  allowed_origins: ["http://localhost:3000", "http://frontend:3000", "http://10.2.142.123:3456"]
  allowed_methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
  allowed_headers: ["*"]